# HTTP
requests>=2.31.0
httpx>=0.27.0

# Redis (EdgePulse distributed bus — requires redis>=4.2 for redis.asyncio)
# Pinned: redis 7.4.0 is the last confirmed version without socket_keepalive regression
redis>=7.4.0,<8.0

# Auth
cryptography>=42.0.0

# Config
python-dotenv>=1.0.0

# Models
scipy>=1.13.0

# WebSocket
websocket-client>=1.7.0

# Dashboard
flask>=3.0.0
streamlit>=1.35.0

# Alerts (optional)
twilio>=8.0.0

# EdgePulse additions
anthropic>=0.40.0           # LLM policy agent (llm_agent.py)
prometheus-client>=0.20.0   # Metrics server (ep_metrics.py)
asyncpg>=0.29.0             # Postgres audit writer (ep_pg_audit.py)
structlog>=24.0.0           # Structured JSON logging
